-if (story) {
  -if (linkTitle) {
    h2
      a(href='/page/' + story.name)
        != story.title
  -} else {
    h1!= story.title
  -}
  p.when!= fn.fromNow(story.dateUpdated||story.dateCreated)
-}
