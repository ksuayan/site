extends ../layout

block content
  #content
    .container.article
      .row
        .col-lg-8
          h1!= content.title
          != content.body
        .col-lg-4
          for component in components
            .col-sm-12
              +#{component.template}(component)