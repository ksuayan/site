{
  "name": "redsugar",
  "version": "1.0.0",
  "description": "Blood Sugar Processing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "yarn run build && node dist/index.js"
  },
  "keywords": [
    "redsugar",
    "nodejs",
    "blood",
    "glucose",
    "es6"
  ],
  "author": "Kyo Suayan",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "babel-preset-env": "^1.7.0",
    "cheerio": "^1.0.0-rc.2",
    "moment": "^2.22.2",
    "querystring": "^0.2.0",
    "simple-statistics": "^6.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
